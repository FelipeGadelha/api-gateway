version: "3.7"

networks:
  kong-net:
    name: kong-net
    driver: bridge
   
services:
  convert-api:
    build:
      context: ./ms/convert
      dockerfile: ./Dockerfile
    image: convert/api
    ports:
      - 8081:8080
    networks:
      - kong-net